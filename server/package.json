{
  "name": "example-apollo-graphql",
  "private": true,
  "author": "mryhryki",
  "scripts": {
    "start": "esbuild --bundle --platform=node --target=node18 ./index.ts | node",
    "dev": "nodemon --watch ./ --ext ts --exec 'npm start'",
    "lint": "mryhryki-lint --directory ./",
    "lint:fix": "mryhryki-lint --directory ./ --fix",
    "type": "tsc",
    "type:watch": "tsc --watch"
  },
  "dependencies": {
    "@apollo/server": "^4.6.0",
    "@mryhryki/lint": "^0.0.12",
    "esbuild": "^0.17.15",
    "graphql": "^16.6.0",
    "nodemon": "^2.0.22",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.0.3"
  }
}
